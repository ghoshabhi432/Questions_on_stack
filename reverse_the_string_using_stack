#include <bits/stdc++.h>
using namespace std;
int main() {
	// your code goes here
	string s;
	cin>>s;
	stack<char> st;
	for(int i=0;i<s.size();i++){
	    st.push(s[i]);
	}
	string ans="";
	while(st.size()!=0){
	    ans.push_back(st.top());
	    st.pop();
	}
	
	cout<<ans;
	
	return 0;
}
